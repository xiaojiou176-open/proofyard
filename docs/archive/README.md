# Archive Boundary

This directory marks the boundary between current public guidance and historical
material.

Archive content can exist for traceability, but it must never compete with the
current storefront path.

If a document belongs to the archive, the current public route should already
point to a newer replacement from `README.md` or `docs/index.md`.
