# Apps Guide

This file covers the application layer under `apps/`.

## Entry points

- `apps/api/` - backend API
- `apps/web/` - web UI
- `apps/automation-runner/` - automation pipeline
- `apps/mcp-server/` - MCP adapter
